using WeatherForecast.Api.Helpers;
using WeatherForecast.Api.Domain;

namespace WeatherForecast.Test.Domain;

public class WeatherForecastTest
{
//    [Fact]
//    public void Create_weather_forecast_record_return_record()
//    {
//        var forecast = new Forecast
//        (
//            DateOnly.FromDateTime(DateTime.Now),
//            43,
//            Constants.Summaries[Random.Shared.Next(Constants.Summaries.Length)]
//        );

//        Assert.True(forecast.GetType() == typeof(Forecast));
//        Assert.Equal(forecast.Date, DateOnly.FromDateTime(DateTime.Now));
//        Assert.Equal(43, forecast.TemperatureC);
//       // Assert.Equal(109, forecast.TemperatureF);
//        Assert.Contains(forecast.Summary, Constants.Summaries);
//    }
}